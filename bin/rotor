#!/usr/bin/env ruby
# frozen_string_literal: true

require "motor"

def main
  xlsx = Motor::Reader::XLSX.new(ARGV.first)
  problem = Motor::Model::Problem.create(xlsx)
  puts Motor::Writer::JSON.(problem)
end

main
