#!/usr/bin/env ruby
# frozen_string_literal: true

require "motor"

xlsx = Motor::Reader::XLSX.new(ARGV.first)
# p xlsx.constraints.to_a
problem = xlsx.call
# puts problem.to_json
puts Motor::Writer::JSON.(problem)
# p problem.constraints["Toplam Arazi"]["Buğday (Sulu)"]
